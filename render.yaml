services:
  - type: web
    name: juridia-api
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true
    # Las variables de entorno se gestionan manualmente en el dashboard de Render
    # para evitar exponer secretos en el repositorio.
